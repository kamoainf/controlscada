version: '3.8'

services:
  backend:
    build: .
    ports:
      - "8080:8080"
    environment:
      - NODE_ENV=production
      - PORT=8080
    volumes:
      - kamoa_auth:/tmp/kamoa_auth
    restart: unless-stopped

volumes:
  kamoa_auth:
